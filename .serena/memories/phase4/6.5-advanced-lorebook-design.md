# Phase 4 · 6.5 — Advanced Lorebook (spec + plan written, awaiting execution choice)

## Status
- Branch created: `milestone/6.5-advanced-lorebook` (off main).
- Spec written: `docs/superpowers/specs/2026-06-15-advanced-lorebook-design.md` (gitignored — docs/superpowers/ is in .gitignore, so specs/plans are local-only, consistent with prior milestones).
- Plan written: `docs/superpowers/plans/2026-06-15-advanced-lorebook.md` (7 TDD tasks).
- Next: user picks execution mode (subagent-driven recommended, or inline executing-plans). Nothing implemented yet.

## Key codebase findings (already-built backend)
- `internal/lorebook/models.go`: `Entry` ALREADY has `Characters []string`, `Selective`, `SelectiveLogic`,
  `Probability`, `UseProbability`, `Order`. JSON tags match SillyTavern world_info spec. Helpers NewEntry/NextUID/ExportWorldInfo.
- `internal/cardexport/card.go`: export-time scoping ALREADY wired — `filterEntries` keeps entry when
  `len(e.Characters)==0 || slices.Contains(e.Characters, charKey)`. Options EmbedLorebook, ScopePerChar.
- `internal/cardexport/export.go`: **charKey = `fmt.Sprintf("%d", ch.ID)`** → scoping UI stores character ID
  STRINGS in entry.characters.
- Frontend LorebookScreen.tsx: selective-logic segment + probability slider ALREADY present (2 of 5 bullets done).
- `GetCharacters() []compose.Character` (app.go:299) exposes id+name. Dialogs in `project_manager.go` via
  `runtime.OpenFileDialog`/`FileFilter` (PickOpenBundle pattern), `p.ctx()` lazy ctx.
- Lorebook bindings GetLorebook/SaveLorebook/ExportLorebook at app.go ~723-744.
- Frontend test mock pattern: vi.mock('../../wailsjs/go/main/App', ...) in LorebookScreen.test.tsx.

## Decisions (user)
1. Import: ask each time (merge vs replace prompt).
2. Scoping: character chips, empty = global.
3. Reorder: reassign `order` with gaps (top=1000, step -10).

## Plan tasks (TDD, each commits)
1. `internal/lorebook/import.go` ParseWorldInfo([]byte)->([]Entry,error): handles {entries:{uid:{}}} map,
   {entries:[...]} array, bare array; sorted asc by UID; empty->nil,nil; malformed->error.
2. project_manager.go PickLorebookFile() dialog + app.go ImportLorebook() (picks/reads/parses, no state mutation,
   cancel->nil,nil). Then `wails generate module` to regen bindings.
3. frontend/src/utils/lorebook.ts pure helpers reorderByDrag/remapForMerge/renumberFromZero + tests.
4. Scope chips in LbDetail (store String(c.id)); load via GetCharacters.
5. HTML5 drag-reorder list (disabled while search active); onDrop -> reorderByDrag -> persist.
6. Import button in PageHead + merge/replace overlay prompt; merge=remapForMerge append, replace=renumberFromZero.
7. Quality gate + ROADMAP update (mark 6.5 done, add 2026-06-15 progress block).

## Quality gate (AGENTS.md)
go vet; golangci-lint run; go test ./... -race -cover; cd frontend && npm run lint && npm run test:coverage;
wails build -clean -tags webkit2_41. >=80% coverage, 0 warnings. Milestone workflow: APPROVAL_REQUEST.md gate
before push, no push until user approves.
