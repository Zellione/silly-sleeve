# 6.7 — Import existing cards (design + plan ready)

Last remaining Phase 4 item. **Branch `milestone/6.7-import-cards` created** off main.
Roadmap-start committed (8cc1189: `chore(roadmap): start 6.7 import existing cards`).
Design spec: `docs/superpowers/specs/2026-06-23-import-existing-cards-design.md` (gitignored, local).
Implementation plan: `docs/superpowers/plans/2026-06-23-import-existing-cards.md` (gitignored, local) — 6 TDD tasks.
**Status: awaiting user's execution-choice (subagent-driven vs inline) before coding tasks 1-6.**

## User decisions (locked)
- Import target: Add to current project (new character + merge character_book into project lorebook).
- Field mapping: Smart `### ` heading parser with fallback (no recognized headings → whole description into Backstory).
- Formats: PNG v2/v3 (tEXt) + JSON (v1/v2/v3).
- UI entry point: Editor character strip ("Import card" button beside add/delete).
- User instructions: use serena symbolic tools when editing; branch before writing code (done).

## Verified codebase facts (confirmed by reading)
- New pkg MUST be `cardimport` (not `import` — TS reserved word).
- `cardexport.ReadTextChunks(pngData)` exists (png.go) — reuse for PNG. Also `cardexport.InjectTextChunk`, `BuildCharacterPNG`, `CardV2JSON`, `CardV3JSON`, `Options{EmbedLorebook,ScopePerChar,IncludeGreetings,StripMetadata}`.
- Silly Sleeve base64-encodes tEXt values on export (export.go) → import base64-decodes with raw-JSON fallback.
- Export lossy: `compose.BuildCardFields` (compose/export.go) concatenates Appearance/Personality/Backstory/Abilities & Skills/Relationships/Stats into markdown `description` with `### Heading`; Stats as `- **k**: v`; first_mes=quotes[0]; mes_example wraps rest in `<START>/{{user}}/{{char}}:`; creator_notes=Epithet.
- `compose.Character`: id,name,epithet,tags[],appearance,personality,backstory,abilities,relationships,quotes[],stats[]StatKV{key,value},portrait[]byte,sourceUrl. `compose.NewCharacter(id)` defaults Name="Untitled", Quotes=[""], Stats=[{"",""}].
- `lorebook.Entry`, `lorebook.NewEntry(uid)`, `lorebook.NextUID(entries)`, `lorebook.ParseWorldInfo` exist.
- **App lorebook field is `a.lorebookEntries`** (NOT a.lorebook). `a.nextCharID()` (app.go:418). `a.characters`, `a.activeCharID`, `a.mu`. `AddCharacter` mutates+returns. `ImportLorebook` (app.go:786) dialog→read→parse, returns nil,nil on cancel. App imports already include compose, lorebook, cardexport NOT yet — add `silly-sleeve/internal/cardimport`.
- `ProjectManager.PickLorebookFile` (project_manager.go:72) is the OpenFileDialog pattern; add `PickCardFile` (filter `*.png;*.json`).
- Frontend EditorScreen.tsx: `CharacterStrip` component (~line 212) renders tabs + `.ss-char-add` button via `onAdd`; add `onImport`. Handlers `handleAdd`/`handleDelete` (~351) use `refreshCharacters()`, `setActiveChar`, `useToast().toast({kind,title,body})`. Bindings imported from `../../wailsjs/go/app/App` (regenerate with `wails generate module` after adding ImportCard).

## Planned API (in plan doc)
- `cardimport.ParseCard([]byte)(*ParsedCard,error)`; `ParsedCard`, `CharacterBook`, `BookEntry{...Enabled *bool...}`.
- `cardimport.ToCharacter(*ParsedCard)(compose.Character,[]lorebook.Entry)`.
- `App.ImportCard()(*ImportCardResult,error)` + unexported `importCardData([]byte)` (testable w/o dialog); `ImportCardResult{Character compose.Character; ImportedEntries int}` → JSON `character`/`importedEntries`.
- `ProjectManager.PickCardFile()`.

## Plan task TODOs to resolve at execution time
- Confirm app_test.go helper names (`newTestApp`? `GetLorebook`/`SetLorebook`?) before writing Task 4 tests.
- Copy existing EditorScreen test mock surface for Task 5 (don't invent binding names).

## Known lossy edges (document, don't fix)
mes_example `{{user}}: ...` wrapper, exact stats whitespace, scenario (always empty on export).
