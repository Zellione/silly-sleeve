# SonarCloud findings on PR #87 (branch `feature/ui-redesign`)

Fixed in `cb0279b` — all 4 open PR findings cleared in one commit:

- **css:S4656** (style.css `.character-card .title-block h2`): duplicate
  `letter-spacing` (-0.02em shadowed by -0.01em). This was the only
  reliability-rated finding and single-handedly pushed the PR quality gate to
  ERROR (`new_reliability_rating` 3). Kept the effective `-0.01em`.
- **typescript:S3776** (SummariesScreen card `list.map` callback, complexity
  18/15): fixed by extracting the `sentLabel(isSent, link, isChar)` helper
  (the if/else + nested ternary block was ~6-8 points) and a module-private
  `SummaryBody` component (editing textarea vs infobox+sections). Foot stayed
  inline. DOM unchanged → all 20 SummariesScreen tests passed untouched.
- **typescript:S7744**: `{ ...(x ?? {}), k: v }` → `{ ...x, k: v }` (spreading
  undefined is legal; the `?? {}` fallback is the finding).
- **typescript:S7750**: `.filter(Boolean).pop()` → `.findLast(Boolean)`
  (pageSlug.ts). Type-checks fine on this tsconfig.

## Notes

- Sonar scores S3776 **per function** — the map callback was flagged, not the
  enclosing component, so moving JSX into private components/helpers is a real
  fix, not a shuffle. Private (non-exported) components don't trip
  `react-refresh/only-export-components`.
- A stale TS LSP diagnostic claimed `UpdateCrawlSummary` missing from wailsjs
  bindings right after this session's regeneration; `tsc --noEmit` on disk was
  clean. Trust tsc over a stale LSP after `wails generate module`.
- Quality-gate reading tip: `new_reliability_rating` ERROR with an otherwise
  clean-looking list means look for BUG-type rules (css:S4656 counts) — the
  MCP issue list doesn't show the type column.
