# SonarCloud findings on PR #97 (branch `feature/llmtest-harness`)

Cleared in two commits; gate OK, 0 open issues at the end.

- **go:S3776 analyze.go `analyzeRun`** (19/15): extracted `exchangeProblems(s, ex) []string`
  judging one raw exchange; analyzeRun became an aggregation loop.
- **go:S3776 consistency.go `compareSummaries`** (20/15): took TWO passes.
  First extraction (`intVarianceMessage` for the int case) only got it to
  **16/15** — Sonar re-scored above threshold again. Second pass moved the whole
  per-key type dispatch into `summaryKeyFindings(scenarioID, runs, key)`,
  leaving compareSummaries as a plain collect loop. Lesson: when a function is
  well over (20), extract the *entire* dispatch/branch structure, not just the
  heaviest case, or budget for a second round trip through automatic analysis.
- **go:S1192 fixtures.go**: `"Mira Dawnhollow"` ×3 → `fixtureCharacterName` const.

Workflow notes:
- SonarQube MCP tools were not connected this session; the public SonarCloud
  web API works unauthenticated for this public project:
  `curl "https://sonarcloud.io/api/issues/search?componentKeys=Zellione_silly-sleeve&pullRequest=<N>&statuses=OPEN,CONFIRMED"`
  and `.../api/qualitygates/project_status?projectKey=...&pullRequest=<N>`.
- Wait for re-analysis of the NEW head before re-querying:
  `gh api repos/Zellione/silly-sleeve/commits/<sha>/check-runs --jq '...Sonar...'`.
- zsh gotcha: `status` is a read-only zsh variable — don't use it as a shell
  variable name in polling scripts (`sonar_state` etc. instead).
